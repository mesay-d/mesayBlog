# Enforce consistent line endings in the repository
# Treat all files as text and normalize line endings to LF in the repo
* text=auto eol=lf

# Explicit rules (optional): keep binary files untouched
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.gz binary
*.woff2 binary

# End of file
